version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: todo_db
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: truonglx
      MONGO_INITDB_ROOT_PASSWORD: truonglx0603
    ports:
      - "27017:27017"
    volumes:
      - todo_data:/data/db

volumes:
  todo_data: